spring:
  application:
    name: luma-service
  #Spring Boot Admin Dashboard
  boot:
    admin:
      client:
        enabled: false
  # Spring Config Service
  cloud:
    config:
      uri:

security:
  jwt:
    signing:
      key: signingKey